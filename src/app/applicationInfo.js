/**
 * Static identity for the standalone PIPELINE application.
 * PIPELINE is part of the OCG ecosystem but is NOT OCG ONE.
 */
export const applicationInfo = Object.freeze({
  name: "OCG PIPELINE",
  service: "pipeline",
  version: "0.1.0",
  ecosystem: "OCG",
  integrationContractVersion: "0.0.0-draft",
});
